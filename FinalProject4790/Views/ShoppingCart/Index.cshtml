@model ShoppingCartViewModel

<h2>Shopping Cart</h2>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Selected amount</th>
            <th>Product</th>
            <th class="text-right">Price</th>
            <th class="text-right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var lineItem in Model.ShoppingCart.ShoppingCartLineItems)
        {
            <tr>
                <td class="text-center">@lineItem.LineItemQuantity</td>
                <td class="text-left">@lineItem.LineItemProduct.ProductName</td>
                <td class="text-right">@lineItem.LineItemProduct.ProductPrice.ToString("c")</td>
                <td class="text-right">
                    @((lineItem.LineItemQuantity * lineItem.LineItemProduct.ProductPrice).ToString("c"))
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" class="text-right">Total:</td>
            <td class="text-right">
                @Model.ShoppingCartTotal.ToString("c")
            </td>
        </tr>
    </tfoot>
</table>